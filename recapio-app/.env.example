# Recapio — Environment Variables
# Copy this file to .env and fill in the values.

# PostgreSQL connection string (Neon or Supabase recommended for MVP)
DATABASE_URL="postgresql://user:password@host:5432/recapio?sslmode=require"

# AI Credentials
OPENAI_API_KEY="your-openai-api-key"
ANTHROPIC_API_KEY="your-claude-api-key"

# Cloud Storage (Optional - Required for Vercel Serverless deployments. Default fallback: local filesystem)
SUPABASE_URL="https://your-supabase-project.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="your-supabase-service-role-key"
SUPABASE_BUCKET="audios" # Must be created with "public" access enabled inside Supabase console

